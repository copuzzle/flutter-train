import 'package:flutter/material.dart';

class NewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text("feed-新的界面"),
        ),
        body: Center(
          child: Text("我是一个新的界面"),
        ),
      );
}
